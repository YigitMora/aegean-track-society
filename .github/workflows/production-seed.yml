name: Production Catalog Seed

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: "Type SEED PRODUCTION"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: production-database
  cancel-in-progress: false

jobs:
  authorize:
    name: Authorize current main catalog seed
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      approved_sha: ${{ steps.authorize.outputs.approved_sha }}

    steps:
      - name: Verify trusted main dispatch and confirmation
        id: authorize
        shell: bash
        env:
          CONFIRMATION: ${{ inputs.confirmation }}
        run: |
          test "$GITHUB_REF" = "refs/heads/main"
          [[ "$GITHUB_SHA" =~ ^[0-9a-f]{40}$ ]]
          test "$CONFIRMATION" = "SEED PRODUCTION"
          echo "approved_sha=$GITHUB_SHA" >> "$GITHUB_OUTPUT"

      - name: Record authorized operation
        shell: bash
        run: |
          {
            echo "### Authorized production catalog seed"
            echo "- Actor: $GITHUB_ACTOR"
            echo "- Commit: $GITHUB_SHA"
            echo "- Run: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
          } >> "$GITHUB_STEP_SUMMARY"

  seed:
    name: Seed production catalog
    needs: authorize
    runs-on: ubuntu-latest
    environment: Production
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ needs.authorize.outputs.approved_sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Verify checked-out commit is current main
        shell: bash
        run: |
          git fetch --no-tags origin main
          CHECKED_OUT_SHA="$(git rev-parse HEAD)"
          REMOTE_MAIN_SHA="$(git rev-parse origin/main)"
          test "$CHECKED_OUT_SHA" = "$GITHUB_SHA"
          test "$CHECKED_OUT_SHA" = "$REMOTE_MAIN_SHA"

      - name: Set up pnpm
        uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa # v4
        with:
          version: 11.7.0

      - name: Set up Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Seed production catalog
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          test -n "$DATABASE_URL"
          pnpm seed:catalog
