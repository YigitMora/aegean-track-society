name: Production Database Seed

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: "Type SEED PRODUCTION followed by the exact 40-character main SHA"
        required: true
        type: string
      seed_package_sep20_price:
        description: "SEP20 package price used by prisma db seed"
        required: true
        type: string
      seed_package_sep20_capacity:
        description: "SEP20 package capacity used by prisma db seed"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: production-database
  cancel-in-progress: false

jobs:
  authorize:
    name: Authorize exact main commit
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      approved_sha: ${{ steps.authorize.outputs.approved_sha }}

    steps:
      - name: Verify trusted main dispatch and exact confirmation
        id: authorize
        shell: bash
        env:
          CONFIRMATION: ${{ inputs.confirmation }}
        run: |
          test "$GITHUB_REF" = "refs/heads/main"
          [[ "$GITHUB_SHA" =~ ^[0-9a-f]{40}$ ]]
          test "$CONFIRMATION" = "SEED PRODUCTION $GITHUB_SHA"
          echo "approved_sha=$GITHUB_SHA" >> "$GITHUB_OUTPUT"

      - name: Record authorized operation
        shell: bash
        run: |
          {
            echo "### Authorized production database seed"
            echo "- Actor: $GITHUB_ACTOR"
            echo "- Commit: $GITHUB_SHA"
            echo "- Run: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
          } >> "$GITHUB_STEP_SUMMARY"

  seed:
    name: Seed production reference data
    needs: authorize
    runs-on: ubuntu-latest
    environment: Production
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ needs.authorize.outputs.approved_sha }}
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: 11.7.0

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify seed inputs
        env:
          SEED_PACKAGE_SEP20_PRICE: ${{ inputs.seed_package_sep20_price }}
          SEED_PACKAGE_SEP20_CAPACITY: ${{ inputs.seed_package_sep20_capacity }}
        run: |
          test -n "$SEED_PACKAGE_SEP20_PRICE"
          test -n "$SEED_PACKAGE_SEP20_CAPACITY"

      - name: Seed production data
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          SEED_PACKAGE_SEP20_PRICE: ${{ inputs.seed_package_sep20_price }}
          SEED_PACKAGE_SEP20_CAPACITY: ${{ inputs.seed_package_sep20_capacity }}
        run: |
          test -n "$DATABASE_URL"
          pnpm prisma:seed
