import { AccountLoadingState } from "@/components/account-loading-state";

export default function AccountLoading() {
  return <AccountLoadingState eyebrow="Üye alanı" title="Hesabınız yükleniyor." />;
}
